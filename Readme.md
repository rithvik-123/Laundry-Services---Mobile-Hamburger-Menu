# Laundry Services - Mobile Hamburger Menu

## Description
This project implements a responsive mobile navigation system for the Laundry Services web application. It features a hidden hamburger menu for mobile devices that triggers a side-menu overlay. The interactive toggle is built entirely using pure CSS and the `:focus` pseudo-class on a hidden button, completely avoiding the use of JavaScript.